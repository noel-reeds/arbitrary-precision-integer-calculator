#include "../arbitrary.h"
/**
 * free - free memory in program after use.
 *
 *@arr: array of pointers previously allocated memory.
 *
 * Return: returns void.
 */
void arbitrary_free(void *arr[]) {
	exit(EXIT_SUCCESS);
}
